<div class="container bloc_test">
	<div class="row">
		<div class="col-sm-10 col-md-8 col-lg-6 mb-5 mx-auto d-table">
			<div class="d-table-cell align-middle">
				<h1 class="text-center m-5">Create new Idea</h1>
				<form class="form-idea text_user form m-3 p-3 shadow bg-light" method="post" action="#anchor">
					{{ form_start(form) }}
					{{ form_widget(form) }}
					<div class="bloc-btn">
						<button class="btn btn-save-marge">{{ button_label|default('send the idea') }}</button>
					</div>
					{{ form_end(form) }}
				</form>
			</div>
		</div>
	</div>
</div>
