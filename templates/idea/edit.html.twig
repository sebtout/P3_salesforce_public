{% extends 'base.html.twig' %}

{% block title %}Edit Idea
{% endblock %}

{% block body %}
	<div class="container bloc_test">
		<div class="row">
			<div class="col-sm-10 col-md-8 col-lg-6 mb-5 mx-auto d-table">
				<div class="d-table-cell align-middle">
					<h1 class="text-center m-5">Edit Idea</h1>
					<form class="form-idea form m-3 bg-lightgrey p-3 shadow bg-light" method="post" action="#anchor">
						{{ form_start(form) }}
						{{ form_widget(form) }}
						<div class="bloc-btn">
							<button class="btn btn-save-marge">{{ button_label|default('update') }}</button>
						</div>
						{{ form_end(form) }}
					</form>
				</div>
			</div>
		</div>
	</div>
	<div class="bloc-btn">
		<button class="btn btn-save-marge" href="{{ path('app_idea_index') }}">back to list</a>
	</div>
{% endblock %}
